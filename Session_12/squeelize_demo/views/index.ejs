<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<!--  -->
<style>
    body {
        font-family: Arial, sans-serif;
        background: #f4f4f4;
        margin: 0;
        padding: 0;
    }
    form {
        background: #fff;
        max-width: 400px;
        margin: 60px auto;
        padding: 32px 24px;
        border-radius: 8px;
        box-shadow: 0 2px 8px rgba(0,0,0,0.08);
        display: flex;
        flex-direction: column;
        gap: 18px;
    }
    form div {
        display: flex;
        flex-direction: column;
        gap: 6px;
    }
    label {
        font-weight: 500;
        margin-bottom: 2px;
    }
    input[type="number"] {
        padding: 8px;
        border: 1px solid #ccc;
        border-radius: 4px;
        font-size: 1rem;
    }
    button[type="submit"] {
        padding: 10px 0;
        background: #007bff;
        color: #fff;
        border: none;
        border-radius: 4px;
        font-size: 1rem;
        cursor: pointer;
        transition: background 0.2s;
    }
    button[type="submit"]:hover {
        background: #0056b3;
    }
</style>
<body>
    <h1>Form Hình Chữ Nhật</h1>
    <form action="/calculateForm" method="POST">
        <div>
            <label for="lenght">Chiều dài:</label>
            <input type="number" id="lenght" name="lenght" required>
        </div>
        <div>
            <label for="width">Chiều rộng:</label>
            <input type="number" id="width" name="width" required>
        </div>
        <button type="submit">Tính</button>
    </form>
</body>
</html>